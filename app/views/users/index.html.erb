<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản lý tài khoản</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  </head>
  <style>
    html,
    body {
      height: 100%;
      margin: 0;
      overflow: hidden
    }
    .container {
      display: flex;
      height: 100%;
      max-width: 100%;
      padding: 0px;
    }
    /* Notification User */
    .user_icon {
      font-size: 24px;
      margin-right: 14px;
      margin-top: 6px;
      cursor: pointer;
      position: relative;
    }
    #userNotifi {
      position: absolute;
      right: 0;
      background-color: white;
      width: 430px;
      box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
      border-radius: 12px;
      top: 120%;
      z-index: 99999;
      height: 200px;
      overflow-y: auto;
      display: none;
    }
    .Notify__block {
      display: block !important;
    }
    .color__primary {
      color: #d72134 !important;
    }
    .notify__error {
      background-color: #d72134 !important ;
    }
    .notifi__Item {
      padding: 10px;
    }
    .notifi__mess {
      background-color: #5cbd4c;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: space-around;
      gap: 20px;
      padding: 14px 22px;
    }
    .mess_content p:first-child {
      color: white;
      font-size: 18px;
    }
    .mess_content p:last-child {
      font-size: 14px;
      color: white;
      margin-top: 10px;
    }
    .notifi__Item i {
      color: white;
    }
    /* Modal */
    .Modal__AddMember {
      display: none !important;
      position: fixed;
      top: 0;
      right: 0;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgb(0 0 0 / 0.5);
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 999;
    }
    .Modal__inner {
      width: 800px;
      background-color: white;
      border: 2px solid black;
      border-radius: 12px;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .Modal__header {
      width: 800px;
      background-color: #dc546c;
      padding: 20px;
      display: flex;
      color: white;
      justify-content: space-between;
      align-items: center;
      border-bottom: 2px solid black;
      font-weight: 500;
    }
    .Modal__header p {
    }
    .Modal__header i {
      font-size: 20px;
      cursor: pointer;
    }
    .modal__body {
      padding: 40px 20px;
      display: flex;
      flex-direction: row;
      justify-content: space-around;
    }
    .body__img {
      position: relative;
      display: flex;
      justify-content: center;
    }
    .body__img button {
      position: absolute;
      top: 20%;
      padding: 10px;
      border: none;
      border-radius: 12px;
      background-color: #dadada;
      color: black;
      font-weight: 600;
      cursor: pointer;
      display: flex;
      align-items: center;
      box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
    }
    .body__img button i {
      margin-right: 6px;
    }
    .body__img span {
      border-radius: 12px;
      width: 200px;
      display: block;
      border: 2px solid #dadada;
      height: 200px;
      box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
    }
    .select__modalbody {
      display: flex;
      width: 100%;
      align-items: center;
      padding: 10px 20px;
      justify-content: space-between;
      border: 2px solid #dadada;
      border-radius: 12px;
      cursor: pointer;
    }
    .select__modalbody p {
      color: black;
      font-weight: 500;
      letter-spacing: 0.3;
    }
    .body__content ul li {
      display: grid;
      grid-template-columns: 30% 70%;
      align-items: center;
      margin-bottom: 30px;
    }
    .body__btn {
      margin-top: 60px;
      display: flex;
      align-items: center;
      gap: 20px;
    }
    .btn__modal {
      width: 100%;
      border: none;
      outline: none;
      padding: 12px 0px;
      cursor: pointer;
      font-weight: bold;
      color: white;
      border-radius: 12px;
    }
    .btn__modal:first-child {
      background-color: #dc546c;
    }
    .btn__modal:last-child {
      background-color: #6c8de0;
    }
    /* Select  */
    .select__modalbody {
      position: relative;
    }
    .option__modalbody {
      position: absolute;
      border: 2px solid #dadada;
      border-bottom-left-radius: 12px;
      border-bottom-right-radius: 12px;
      box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      top: 100%;
      left: 0;
      width: 100%;
      background-color: white;
      z-index: 10;
      display: none;
      user-select: none;
    }
    .option__modalbody p {
      padding: 14px 20px;
      font-weight: 500;
    }
    .option__modalbody p:hover {
      background-color: #dadada;
    }
    .show__option {
      display: block !important;
    }
    .show__modal {
      display: flex !important;
    }
    .radiusSelect {
      border-bottom-left-radius: 0px;
      border-bottom-right-radius: 0px;
    }
    .generality__options {
      display: flex;
      align-items: center;
    }
    .btn__addmember button {
      display: flex;
      align-items: center;
      padding: 14px 16px;
      border: none;
      outline: none;
      background-color: blue;
      color: white;
      cursor: pointer;
      border-radius: 12px;
    }
    .btn__addmember button p {
      margin-left: 6px;
    }
    .btn__addfile {
      margin: 0px 24px;
    }
    .btn__addfile button {
      display: flex;
      align-items: center;
      padding: 14px 16px;
      border-radius: 12px;
      border: none;
      color: white;
      background-color: green;
      cursor: pointer;
    }
    .btn__addfile button p {
      margin-left: 6px;
    }
    .select__options select {
      padding: 8px 6px;
      border-radius: 10px;
    }
    .container__info {
      height: 690px;
      width: 100%;
      border-radius: 14px;
      overflow: hidden;
      position: relative;
    }
    .info__img {
      background-image: url("../app/assets/images/Bird.jpg");
      width: 20%;
      float: left;
      height: 100%;
      background-repeat: no-repeat;
      background-size: cover;
      background-position: center;
    }
    .info__content {
      background-color: white;
      padding: 100px 0px 100px 100px;
      float: right;
      width: 80%;
      height: 100%;
    }
    .info__title {
      margin-left: 32px;
    }
    .info__title h1 {
      padding-bottom: 6px;
    }
    .info {
      font-size: 16px;
      color: gray;
      margin: 6px 0px 40px 0px;
    }
    .info__list li {
      display: flex;
      align-items: center;
      list-style: none;
      font-size: 16px;
      margin: 40px 0px;
    }
    .info__list li p {
      margin: 0px 50px 0px 20px;
      color: gray;
      font-size: 18px;
      text-align: left;
    }
    .info__list li span {
      width: 12px;
      height: 12px;
      border: 1px solid gray;
      border-radius: 50%;
    }
    .info__user_img {
      position: absolute;
      top: 50px;
      left: 190px;
      border-radius: 50%;
      border: 4px solid white;
      width: 164px;
      height: 164px;
    }
    .info__user_img img {
      width: 100%;
      height: 100%;
      border-radius: 50%;
    }
    .btn_edit-info {
      position: absolute;
      top: 20px;
      left: 20px;
    }
    .btn_edit-info button {
      padding: 10px 20px;
      border: none;
      cursor: pointer;
      outline: none;
      background-color: white;
      border-radius: 40px;
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    /* MENU */
    .menu {
      background-color: white;
      width: 100px;
      max-height: 100%;
      box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      transition: all 0.5s;
      display: block;
      position: relative;
    }
    .menu__title {
      text-align: center;
      font-size: 14px;
      font-weight: bold;
    }
    .menu__logo {
      width: 100%;
      padding: 20px;
    }
    .menu__logo img {
      width: 100%;
    }
    .menu__items {
      width: 100%;
      text-align: center;
      padding: 20px 0px 20px;
      cursor: pointer;
      color: #dadada;
      display: block;
    }
    .menu__icon i {
      font-size: 20px;
    }
    .menu__icon {
      padding: 6px 0px;
    }
    .menu__active {
      border-left: 2px solid #d72134;
      color: #d72134 !important;
    }
    /* Content */
    .content {
      background-color: #F8F8F8;
      width: 100%;
      padding: 30px 60px 20px 30px;
      overflow-Y: scroll
    }
    .content__title {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .content__name {
      padding: 40px 0px 10px 40px;
      font-size: 30px;
    }
    .content__breadcrumb {
      font-size: 16px;
      font-weight: bold;
      margin-left: 20px;
    }
    .breadcrumb__name {
      display: inline-block;
      margin-left: 10px;
      color: #f34f68;
      padding: 12px 0px;
    }
    .content__user {
      display: flex;
      align-items: center;
    }
    .user_icon {
      font-size: 24px;
      margin-right: 14px;
      margin-top: 6px;
    }
    .user__info {
      margin-right: 10px;
      font-size: 16px;
    }
    .user__info p:first-child {
      text-align: right;
    }
    .user__img img {
      border-radius: 50%;
      width: 40px;
      height: 100%;
    }
    .user__img {
      margin-right: 10px;
    }
    .user__detail {
      font-size: 20px;
      cursor: pointer;
      color: gray;
    }
    .content__generality {
      display: flex;
      align-items: center;
      justify-content: space-between;
      width: 100%;
      margin: 20px 0px;
      padding: 14px 20px;
      background: #f1f1f1;
      border-radius: 8px;
    }
    .generality__total {
      display: flex;
      align-items: center;
    }
    .generality__select {
      margin-left: 10px;
      margin-right: 20px;
    }
    .generality__select select {
      width: 60px;
      height: 40px;
      border-radius: 4px;
      outline: none;
    }
    .content__btn {
      box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      border-radius: 10px;
    }
    .content__btn button {
      cursor: pointer;
      padding: 12px 20px;
      outline: none;
      border-radius: 10px;
      font-size: 16px;
      background-color: white;
      border: 1px solid gray;
      color: black;
    }
    .content__btn button i {
      margin-left: 6px;
    }
    .generality__search {
      position: relative;
    }
    .content__table {
      width: 100%;
      text-align: center;
      border: none;
      border-spacing: 0px;
      border-radius: 12px;
      overflow: hidden;
      margin-top: 25px;
    }
    .content__table th {
      color: white;
      padding: 20px 10px;
    }
    .content__table-checkbox {
      margin-top: 4px;
    }
    .content__table-checkbox-report {
      margin-left: 5px;
    }
    .content__table_body {
      font-size: 16px;
      background-color: #E5E5E5;
    }
    th {
    background-color: #6dbac7;
    color: white; /* Để làm cho văn bản trắng trên nền đỏ */
    }
    /* Định dạng hàng chẵn */
    tr:nth-child(even) {
    background-color: #F1F1F1;
    }
    /* Định dạng hàng lẻ */
    tr:nth-child(odd) {
      background-color: #E5E5E5;
    }
    .checkbox-row {
      display: flex;
      align-items: center;
    }
    .content__table_body td {
      padding: 10px 0px;
    }
    .approve {
      background-color: #5cbd4c;
      display: inline-block;
      padding: 4px 12px;
      border-radius: 12px;
      color: white;
    }
    .btn {
      padding: 6px 8px;
      cursor: pointer;
      outline: none;
      border: none;
      border-radius: 4px;
    }
    .btn i {
      font-size: 20px;
      color: white;
    }
    .gold {
      background-color: #f4924b;
    }
    .blue {
      background-color: #1b59f8;
    }
    .green {
      background-color: #5cbd4c;
    }
    .yellow{
      background-color: #FFBF24
    }
    .grey{
      background-color: #ABABAB
    }
    .paging {
      display: flex;
      justify-content: end;
      align-items: center;
      margin-top: 20px;
    }
    .paging--active {
      background-color: #d72134 !important;
      color: white !important;
    }
    .paging button {
      padding: 10px 14px;
      margin: 0px 8px;
      border: none;
      background-color: rgb(241, 241, 241);
      cursor: pointer;
      font-size: 16px;
      border-radius: 10px;
      outline: none;
    }
    .menu__dropwdown {
      width: 250px;
      box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      position: relative;
      padding: 2px;
      display: none;
      transition: all 0.5s;
    }
    .menu__dropdown_logo {
      padding: 30px;
      width: 100%;
    }
    .menu__dropdown_logo img {
      width: 100%;
    }
    .menu__dropwdown .menu__title {
      text-align: left;
      padding: 0px 20px;
    }
    .menu__dropwdown .menu__items {
      text-align: left;
      margin: 0px;
      padding: 10px 0px;
      display: block;
      text-decoration: none;
    }
    .menu__dropwdown .menu__items .menu__icon {
      display: flex;
      align-items: center;
      padding: 0px 20px;
      justify-content: space-between;
    }
    .menu__dropwdown .menu__items .menu__icon .menu__dropdown_icon {
      display: flex;
      align-items: center;
    }
    .menu__dropwdown .menu__items .menu__icon .menu__dropdown_icon p {
      color: black;
      margin-left: 10px;
      font-size: 18px;
      margin-top: 4px;
    }
    .active {
      color: #d72134 !important;
    }
    .menu__icon_open i {
      font-size: 20px;
      margin-top: 6px;
      transition: all 0.3s;
    }
    .menu__dropdown_nav {
      padding-left: 44px;
      font-size: 14px;
      margin-top: 10px;
      display: none;
      transition: all 0.5s;
    }
    .menu__dropdown_list {
      list-style: none;
    }
    .menu__dropdown_list li {
      color: black;
      padding: 10px 0px;
    }
    .icon__notification {
      width: 15px;
      height: 15px;
      background-color: #d72134;
      border-radius: 50%;
      margin-top: 4px;
    }
    .menu__regime {
      display: flex;
      align-items: center;
      margin: 10px 40px 0px 20px;
      padding: 10px 12px;
      justify-content: center;
      background: #f5f5f5;
      border-radius: 10px;
    }
    .regime__light,
    .regime__dark {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .regime__dark {
      margin-left: 10px;
      background: gray;
      color: black;
      padding: 6px 10px;
      border-radius: 40px;
    }
    .regime__light {
      background-color: white;
      padding: 6px 10px;
      border-radius: 40px;
      color: black;
    }
    .regime__light p,
    .regime__dark p {
      margin-left: 10px;
    }
    /* Toggle */
    #statistics .show {
      display: block;
    }
    .show .menu__icon_open i {
      transform: rotate(-180deg);
    }
    .show ~ .menu__dropdown_nav {
      display: block;
    }
    #btn-dropdown {
      position: absolute;
      left: 234px;
      top: 40px;
    }
    #btn-dropdown button {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background-color: #d72134;
      color: black;
      border: none;
      outline: none;
      cursor: pointer;
      text-align: center;
    }
    #btn-dropdown button i {
      font-size: 16px;
      transition: all 0.5s;
    }
    #btn-menu {
      position: absolute;
      left: 78px;
      top: 30px;
    }
    #btn-menu button {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background-color: #d72134;
      color: black;
      border: none;
      outline: none;
      cursor: pointer;
      text-align: center;
    }
    #btn-menu button i {
      font-size: 16px;
      transition: all 0.5s;
    }
    .select_role {
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }
    .left_role {
      padding-right: 4rem;
    }
    .phoneNumber{
      width: 100%;
      padding: 0.5rem;
      border-radius: 12px;
      border: 2px solid #dadada;
    }
    .btn[data-tooltip]:hover:after {
      content: attr(data-tooltip);
      position: absolute;
      padding: 5px;
      border-radius: 5px;
      left: 100%;
      top: 100%;
      white-space: nowrap;
      z-index: 1;
      background-color: red;
      color: white;    
      overflow: hidden;  
    } 
    .custom-icon{
      position: absolute;
      top: 11px;
      left: 6px;
      font-size: 12px;
      color: gray;
      cursor: pointer;
      padding: 10px;
    }
    .custom-text-field{
      padding: 14px 40px;
      box-shadow: rgba(0, 0, 0, 0.25) 0px 0px 20px 0px;
      border: none;
      outline: none;
      border-radius: 6px;
    }
    .select-row {
      display: flex;
      gap: 10px;
      align-items: center;
      justify-content: space-between;
    }
    .select-row span{
      font-weight: 600;
      white-space: nowrap; 
    }
    .select-row p,
    .select-row select,
    .select-row option,
    .total__report {
      font-weight: 600;
      white-space: nowrap; 
    }
    .select-row .select-field {
      padding-left: 0rem;
      width: 100%;
      padding: 10px 15px;
      border-radius: 12px;
      border: 2px solid #dadada;
    }
    .select-row .select-field option {
      background: #fff;
      color: black;
    }
    .hidden {
      display: none;
    }

  </style>
  <body>
    <main class="container">
      <%= render "shared/left_nav"%>
      <div class="content">
        <%= render 'shared/header' %>
        <h1 class="content__name">QUẢN LÝ TÀI KHOẢN</h1>
        <% if current_user.role == 'admin' %>
          <div class="content__generality">
            <div class="generality__options">                   
              <%#<div class="select-row">
                <span>Vai trò</span>
                <select class="select-field" >
                  <option selected value="0">Tất cả</option>
                  <option value="1">Khoa</option>
                  <option value="2">Bộ môn</option>
                  <option value="3">Giảng viên</option>
                  <option value="4">Nhân viên</option>
                  <option value="5">Chưa có</option>
                </select>
              </div> %>        
            </div>
              <div class="generality__search">
                <%= form_with url: users_path, method: :get, class: "d-flex" do %>
                  <i class="fa-solid fa-magnifying-glass custom-icon"></i>
                  <%= text_field_tag "filter", nil, placeholder: "Tìm kiếm tài khoản", class: "custom-text-field"%>
                <% end %>
              </div>
          </div>
        <% end %>
        <table class="content__table" id="table_phongban">
          <tr class="content__table_header">
            <th>
              STT
            </th>
            <th>
              Họ và tên
            </th>
            <th>
              Email
            </th>
            <th>
              Vai trò
            </th>
            <th>
              Khoa
            </th>
            <th>
              Bộ môn
            </th>
            <th>
            </th>
          </tr>
          <% @users.each do |u|%>
          <tr class="content__table_body"  onclick="toggleButtons(this)">
            <td> <%= u.id %> </td>
            <td> <%= u.name %> </td>
            <td> <%= u.email %> </td>
            <td> <%= u.get_role.split(",").join(", ") || "N/A"  %> </td>
            <td>
              <%#<p class="approve" style="background-color: #C4C4C4;color: black;"><%= u.faculty || "N/A" ></p>%>
              <%= u.faculty&.name || "N/A" %>
            </td>
            <td> <%= u.subject&.name || "N/A"  %> </td>
            <td>
              
              <%#<button class="btn grey" aria-label="btn" title="Hiển thị người dùng" data-tooltip="Tooltip Text"><i class="fa-solid fa-eye"></i></button>
              <button class="btn grey hidden" aria-label="btn" title="Ẩn người dùng" data-tooltip="Tooltip Text"><i class="fa-solid fa-eye-slash"></i></button>%>

              <%= link_to edit_user_path(u), style: "text-decoration:none", data: {turbo: false} do %>
                <button class="btn yellow s" aria-label="btn" title="Chỉnh sửa" data-tooltip="Tooltip Text"><i class="fa-solid fa-pen"></i></button>
              <% end %>
            </td>
          </tr>
          <% end %>
        </table>

        <div class="pagination" style="display:flex; justify-content: center; padding-top: 1rem">
          <%== render partial: 'shared/pagination', locals: {pagy: @pagy} %>
        </div>
        <%# <div class="paging d-none">
          <button aria-label="pagin">
          <i class="fa-solid fa-chevron-left"></i>
          </button>
          <button class="paging--active" aria-label="pagin">1</button>
          <button aria-label="pagin">2</button>
          <button aria-label="pagin">3</button>
          <button aria-label="pagin">...</button>
          <button aria-label="pagin">100</button>
          <button aria-label="pagin">
          <i class="fa-solid fa-chevron-right"></i>
          </button>
        </div> %>
      </div>
      
      </div>
    </main>
  </body>
  <!-- JS -->
  <script>
    //Menu drop
    const getEle = (id) =>{
        return document.getElementById(id)
    }
    getEle("statistics").addEventListener("click",()=>{
        getEle("statistics").classList.toggle("show")
    })
    getEle("report").addEventListener("click",()=>{
        getEle("report").classList.toggle("show")
    })
        getEle("userIcon").addEventListener("click",()=>{
          getEle("userIcon").classList.toggle("color__primary")
          getEle("userNotifi").classList.toggle("Notify__block")
        })
      
  </script>
  <!-- Show option -->
  <script>
    const khoa = document.querySelectorAll(".khoa")
    getEle("select__khoa").addEventListener("click",()=>{
        getEle("option__khoa").classList.toggle("show__option")
        getEle("select__khoa").classList.toggle("radiusSelect")
    
    })
    
    khoa.forEach(e=>{
        e.addEventListener("click",(e)=>{
          getEle("selectedKhoaModal").innerHTML = e.target.innerText
        })
    })
    const bomon = document.querySelectorAll(".bomon")
    getEle("select__bomon").addEventListener("click",()=>{
        getEle("option__bomon").classList.toggle("show__option")
        getEle("select__bomon").classList.toggle("radiusSelect")
    
    })
    
    bomon.forEach(e=>{
        e.addEventListener("click",(e)=>{
          getEle("selectedBomonModal").innerHTML = e.target.innerText
        })
    })
    const vaitro = document.querySelectorAll(".vaitro")
    getEle("select_vaitro").addEventListener("click",()=>{
        getEle("option__vaitro").classList.toggle("show__option")
        getEle("select_vaitro").classList.toggle("radiusSelect")
    
    })
    
    vaitro.forEach(e=>{
        e.addEventListener("click",(e)=>{
          getEle("selectedVaitroModal").innerHTML = e.target.innerText
        })
    })
    const donvi = document.querySelectorAll(".donvi")
    getEle("select__donvi").addEventListener("click",()=>{
        getEle("option__donvi").classList.toggle("show__option")
        getEle("select__donvi").classList.toggle("radiusSelect")
    
    })
    
    donvi.forEach(e=>{
        e.addEventListener("click",(e)=>{
          getEle("setededDonvi").innerHTML = e.target.innerText
        })
    })
    
    getEle("btn__addmember").addEventListener("click",()=>{
        getEle("Modal__AddMember").classList.add("show__modal")
    })
    getEle("close__modalIcon").addEventListener("click",()=>{
        getEle("Modal__AddMember").classList.remove("show__modal")
    })
    getEle("btn__closeModal").addEventListener("click",()=>{
        getEle("Modal__AddMember").classList.remove("show__modal")
    })
  </script>

  <script>
    //search dữ liệu trong bảng 
    // Lấy input tìm kiếm và bảng
    const searchInput = document.getElementById('searchInput');
    const table = document.getElementById('table_phongban');
    const rows = table.querySelectorAll('tr');

    // Lưu trạng thái ban đầu của các hàng
    const initialDisplayStates = [...rows].map(row => row.style.display);

    // Sử lý sự kiện tìm kiếm
    searchInput.addEventListener('input', function () {
      const searchTerm = searchInput.value.trim().toLowerCase();
      rows.forEach((row, index) => {
      // Bỏ qua hàng tiêu đề
        if (row.classList.contains('content__table_header')) return;
        const columns = row.querySelectorAll('td');
          let found = false;
          // Kiểm tra nội dung trong cột và ẩn/hiện dựa trên kết quả tìm kiếm
          columns.forEach((column, columnIndex) => {
            if (column.textContent.trim().toLowerCase().includes(searchTerm)) {
              found = true;
              row.style.display = ''; // Hiển thị hàng
            } else if (found === false && columnIndex === columns.length - 1) {
              row.style.display = 'none'; // Ẩn hàng nếu không tìm thấy kết quả
            }
          });
      });
    });

    // Sử lý sự kiện xóa trường tìm kiếm
    searchInput.addEventListener('keyup', function (event) {
      if (event.key === 'Escape' && searchInput.value !== '') {
        searchInput.value = ''; // Xóa nội dung tìm kiếm
        rows.forEach((row, index) => {
          row.style.display = initialDisplayStates[index]; // Hiển thị lại dữ liệu ban đầu
        });
      }
    });

    //Ẩn hiện người dùng
    // function toggleButtons(row) {
    //   var buttons = row.querySelectorAll('.btn');
    //   buttons.forEach(function(button) {
    //     button.classList.toggle('hidden');
    //   });
    // }
  </script>
</html>